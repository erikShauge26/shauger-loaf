rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server writes use the Admin SDK (bypasses these rules).
    match /preorders/{id} {
      allow read, write: if false;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
